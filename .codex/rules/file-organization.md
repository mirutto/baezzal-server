# File Organization Rules

- Use exactly one class per file by default.
- The only class-grouping exceptions are files whose names end with `Dtos` or `Events`. Those files may contain multiple DTO or event-style classes.
- Extension or utility top-level functions may be grouped in files whose names end with `Extensions`.
- Outside of those exceptions, do not place multiple classes in a single file.
